{
  "outputDirectory": "frontend/dist",
  "buildCommand": "cd frontend && npm run build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/api/server.js" },
    { "source": "/(.*)", "destination": "/frontend/dist/index.html" }
  ]
}
